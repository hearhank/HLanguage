# 编译器初始实现语言：Rust

H 的参考编译器（stage0）用 Rust 实现，长期目标是用 H 自举（三参考语言最终全部自举，见 RESEARCH/ 档案）。

选择 Rust 而非 Zig/C++：Rust 拥有最成熟的编译器生态（LLVM/Cranelift 绑定、parser 与错误报告库），内存安全显著降低编译器自身的 bug 密度；作为长期参考实现，Rust 版本会保留维护。考虑过 Zig（哲学与 H 第一参考同源，但编译器生态未稳，其自身仍在 0.x 大改）与 C++（贴近 LLVM 本体但内存安全负担重），均被否决。
