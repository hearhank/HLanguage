# 0003 · 可逆自描述字节格式

字节化格式采用**递归嵌套 + 类型标签**：任何数据可无损转为字节（块 = memcpy，树 = 序列化压平），并可从字节**反序列化恢复原结构**（树 ⇄ 块双向）。格式自描述，跨语言、跨版本可解释。

选择自描述可逆格式而非偏移式（FlatBuffers）扁平布局，因为"传输/存储"的底线是存了能读回来继续当数据操作，且自描述格式天然支撑跨语言互操作与跨版本兼容。代价是体积与重建速度；字节方法层预留压缩/索引优化通道。

备选：偏移/索引式（被否——非自描述、跨版本弱）、单向序列化（被否——存储/传输不可逆）。
