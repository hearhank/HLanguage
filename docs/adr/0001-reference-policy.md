# 参考语言策略：Zig → Rust → TypeScript

H 的每一个设计决策，优先参考 Zig 的实现方式，其次 Rust，再次 TypeScript；仅当三者都无法回答时才由项目自主决策并记录理由。选择这三门语言，是因为它们分别覆盖 H 的三条目标线：Zig（系统编程、零开销、comptime、显式内存管理）、Rust（类型安全、局部推断、工具链成熟度、自举路径）、TypeScript（脚本体验、渐进式类型、快速发布节奏）。

这一优先级是项目所有者设定的硬约束（2026-08-12），任何偏离都必须显式记录。
