# 0005 · 双后端共享 IR，语义一致性为硬承诺

同一份源码两种执行方式：编译器产出原生二进制，解释器作为脚本执行。二者**共享前端与中间表示（IR）**，语义一致性是硬承诺——脚本跑通，编译后行为必须一致。

"写脚本就是写正式代码"是双后端模式唯一站得住的理由；解释器宽松、编译器严格会制造"脚本跑通、编译炸掉"的双标准。共享 IR 同时是调试器与未来 JIT 的公共地基。

备选：两套前端/子集语言（被否——双语言负担）、语义不承诺一致（被否——摧毁核心价值）。
