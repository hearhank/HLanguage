# SPEC × Examples 一致性检查 + 第一阶段就绪审计（2026-08-14）

> 对照 `07-bootstrap-plan.md`（三块计划）检查 SPEC（06 分文件 + 01 + CONTEXT）与 examples（85 编号示例 + 86/87/88）的一致性，甄别难以实现或存在严重漏洞之处，评估第一阶段（最小功能集：语言系统 M0–M4 + 最小外围 M5–M7）就绪度。

## 一、检查发现与处理（C1–C7）

| # | 级别 | 问题 | 裁决/处理 |
|---|---|---|---|
| **C1** | 🔴 阻塞 | 堆上类型构造：12 个示例用 `X.new(alloc, ...)`，但 `new` 原定脚本生成（E1）——最小集不可用 | **`alloc.init` 双形态定案**（用户裁决）：`alloc.init(Foo)`（无参构造——分配 + 字段后续显式赋值，definite assignment 检查）+ `alloc.init(Foo{字段 = 值, ...})`（带参构造器——类型字面量作构造参数，分配 + 初始化）；`X.new(...)` 样板**取消**；12 个示例批量改 `alloc.init`（18/31/33/36/50/54/56/62/65/66/67/68/69/70/72，含 24 空连续类型改字面量 `X{}`） |
| **C2** | 🔴 阻塞 | Allocator 方法集混用：`arena.alloc(n)` / `arena.alloc(T{...})`（旧）/ `alloc.init(T)`（新）三形态 | **方法集定案**：`alloc.alloc(n) &[u8]`（字节分配）+ `alloc.init(T)`/`alloc.init(T{...})`（类型实例，双形态）+ `alloc.deinit()`；旧 `arena.alloc(T{...})` 废弃；字节分配保留（示例 02/27/49 ✅） |
| **C3** | 🟡 边界 | script 块示例（33/36/81）在最小集不可用 | 33/36/81 头部标注「**E1 示例（第三块，最小集不实现）**」；81 注明最小集用内建序列化、script 为定制通道 |
| **C4** | 🟡 语法 | 泛型接口实例化标注（`class Fib: IIterable<i32>`）未入规范 | 06-05 补「接口实例化标注：`class X: I(T1, T2)`（圆括号类型参数，与 `Vec<i32>` 一致）」 |
| **C5** | 🟡 语法 | 多特性标注叠放（`[continuous] [pad]`）未定义 | 06-03 补「连续中括号行叠放，均作用于同一类型」 |
| **C6** | 🟢 过时 | 总纲语法速查：`table(alloc,...)`（M8 已改）/「自动连续内存」表述（H1 已改特性标注）/`new` 样板 | 总纲语法速查更新：`[continuous]` 标注、`alloc.init` 双形态、`Table<i32>.init`、最小集不实现 script |
| **C7** | 🟢 实现 | `alloc.init(T)` 无参构造的「未初始化中间态」——字段逐赋值的初始化状态跟踪 | **definite assignment 定案**（07 计划 M2.5 标注）：任何退出路径前全字段已赋值，否则编译错误；实现归 M2 语义 |

## 二、难以实现 / 严重漏洞甄别（进入第一阶段前已处理或已明确）

1. **堆上类型构造的 definite assignment**（C7）——编译器需跟踪实例字段初始化状态（含 `o` 字段 move 赋值）——M2 语义实现点，已在计划标注（可行：作用域内实例字段初始化状态流分析）
2. **`alloc.init(T{...})` 的类型字面量参数**——`alloc.init` 首参为「类型或类型字面量」——编译器内建分配器方法（comptime 类型参数 + 字段字面量）——M2/M5.1 实现点
3. **script 块类型信息可见范围的实现**（E1）——class 内/namespace 下的类型收集——第三块，不阻塞第一阶段
4. **其余已知风险**（非本阶段阻塞）：重载歧义诊断、期望类型传播、双后端一致性、4 组合测试矩阵——已登记（可行性 review 17 项清单 + `05`）

## 三、第一阶段（最小功能集）就绪结论

**✅ 可以进入第一阶段（语言系统 M0–M4 起步）**——前置条件已满足：

1. **设计充分**：核心语义（语法/类型系统/所有权/错误/序列化/内建/特性标注/比较体系/迭代契约/构造机制/Allocator 形态）全部定案且自洽，无方向性矛盾
2. **计划边界清晰**：脚本生成（E1）与多线程（E2）明确排除在最小集外（07 第四节）；`hc run` 脚本模式属 M3.2（必须实现）——区分明确
3. **示例即规格已同步**：构造形态（alloc.init）、连续标注（[continuous]）、script 示例边界（E1 标注）全部统一——M0.2 示例基线可用
4. **实现点已标注**：definite assignment（M2.5）、alloc.init 双形态（M2/M5.1）

**建议的开工顺序**：M0 地基 → M1 前端（词法含 `[name]` 特性标注/`.name` 推断枚举/`"""` 多行字符串、parser 含 class/enum/interface/namespace/特性标注）→ M2 语义（类型系统/所有权/definite assignment/重载/迭代契约）→ M3 双后端 → M4 运行时与内建 → M5–M7 最小外围。

**遗留（不阻塞第一阶段，登记于 `05`/可行性报告）**：开放问题 #1/#3/#4/#5/#6、K1/K3/K6 系统编程 1.0 范围、惰性迭代/switch 守卫/Send-Sync（1.x）、`05` 后续细化。
