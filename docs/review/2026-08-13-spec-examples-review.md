# H 语言 SPEC 与 Examples Review 报告 + 裁定记录（2026-08-13）

> 本文档是 2026-08-13 编译器视角 review 会话的完整沉淀：第一部分为 review 报告（问题清单 + 影响），第二部分为逐条裁定记录（Q1–Q28，含文档落点）。与 `docs/adr/`、`CONTEXT.md`、`docs/SPEC/` 互为索引；裁定以本清单为准绳。

## 一、审查范围与方法

- **范围**：`docs/SPEC/`（7 份）、`docs/adr/`（8 份）、`CONTEXT.md`、`examples/`（85 个 `.hc` + math.hc + build.zon）
- **视角**：编译器实现者（lexer/parser → 类型检查 → 所有权分析 → 双后端）——聚焦「实现时会卡住/走错路」的问题
- **方法**：grill-with-docs 会话——一次一个问题、附推荐选项、用户裁定后立即记录到 CONTEXT/SPEC/ADR/示例

## 二、Review 报告（问题清单与状态）

### P0 阻断性矛盾（实现方向性冲突，必须先行定案）—— 4 项 ✅ 全部定案

| 编号 | 问题 | 裁定 | 落点 |
|---|---|---|---|
| P0-1 | `o` 显式标注 vs「复杂类型默认拥有」矛盾（12.1 vs Q16 vs 示例） | 所有权按分配来源编译时判定（Q1） | CONTEXT、01 §4/12.1/12.10/12.20、06 §6、ADR-0003/0005 |
| P0-2 | struct「纯值化」规则 vs 示例（struct 含 String/Vec） | 字段集放宽（Q2） | CONTEXT、01 §12.12、06 §2、ADR-0006 |
| P0-3 | String 值语义（隐式深拷贝）违反「无隐式分配」；可 move 又可拷贝 | String = `u8[]` 别名 + 赋值别名共享（Q3/Q3b/Q3c） | CONTEXT、01 §3/12.19、06 §6、ADR-0003 |
| P0-4 | 可移动性谓词未同步到 06（实作依据） | 已同步（Q1 记录时完成） | 06 §6 |

### P1 语法规格缺口（M1 lexer/parser 开工必需）—— 11 项 ✅ 全部定案

| 编号 | 缺口 | 裁定 | 落点 |
|---|---|---|---|
| P1-1 | 运算符优先级/结合性缺失 | Zig 式 14 级表（Q4） | 06 §3、01 §12.2 |
| P1-2 | 字符字面量未定义 | `'x'` = comptime_int（Q5） | 06 §1、01 §12.3 |
| P1-3 | 字符串转义未定义 | Zig 式全集（Q6） | 06 §1 |
| P1-4 | token 清单缺失 + `mut` 占位符 | 清单冻结 + mut 定名（Q7） | 06 §1、01 §12.1、ADR-0005 |
| P1-5 | `test` 块语法缺失 | Zig 式测试块（Q8） | 06 §2、01 §12.1 |
| P1-6 | `if (e!) \|v\| else \|err\|` 缺失 | 必须成对（Q9） | 06 §4、01 §12.6 |
| P1-7 | `while (opt) \|v\|` 缺失 | 捕获形态（Q10） | 06 §4、01 §12.7 |
| P1-8 | `catch {}` 形态未定义 | 仅 `catch \|_\| {}`（Q11，无简写） | 06 §8、01 §12.15、75 示例 |
| P1-9 | `box`/`copy` 归属未定 | 内建函数（Q12） | 06 §5、01 §12.5 |
| P1-10 | `error.NotFound` 语法未定义 | 全局唯一错误名（Q13） | 06 §8、01 §12.15 |
| P1-11 | 四模式类型方法集未定义 | init/write/read/try_read/close（Q14） | 06 §10、01 §12.21 |

### P2 语义缺口（M2/M4 开工必需）—— 12 项 ✅ 全部定案

| 编号 | 缺口 | 裁定 | 落点 |
|---|---|---|---|
| P2-1 | `o` 可否用于标量（Q7 vs 示例 `o i32`） | 仅分配器分配对象（Q15） | 06 §2、01 §12.1、CONTEXT、ADR-0005、4 示例 |
| P2-2 | 指针可空性未定义 | 不可空，`?*T` 表达空态（Q16） | 06 §6、01 §12.10、CONTEXT |
| P2-3 | 接口指针 `*Shape` 表示未定义 | 胖指针 + 自动收窄 + data 参与登记（Q17） | 06 §2、01 §12.16、CONTEXT、ADR-0006 |
| P2-4 | 悬垂演示示例（28）形态不成立 | 容器逃逸 = 唯一悬垂路径（Q18） | 28 示例重写 |
| P2-5 | 线程引用捕获判定算法 + 数据竞争洞 | 绑定/逃逸数据流判定 + 冻结窗口（Q19） | 06 §10、01 §12.21、CONTEXT、ADR-0007 |
| P2-6 | `Future(!T)` vs `Future(T)` 不一致 | `Future(R)`，R 含 `!`（Q20） | 01 §12.23、06 §10、ADR-0008 |
| P2-7 | `defer` 执行顺序未定义 | LIFO（Q21） | 06 §4、01 §12.1、CONTEXT |
| P2-8 | 接口对象传参语义未定义 | where 约束虚拟类型（Q22/Q22b/Q22c） | 06 §2/§5、01 §12.5/12.16/12.18、ADR-0005/0006、19 示例 |
| P2-9 | `script{}` 输入机制（A4）空白 | `types` 元数据对象（Q23） | 06 §9、01 §7、ADR-0006、33/36/81 示例 |
| P2-10 | `&` 三义解析策略 | 单一 token + 上下文消解（Q24） | 06 §3、01 §12.2 |
| P2-11 | 方法链临时 `*mut` 生命周期 | 存活到语句结束 = reborrow 延续（Q25） | 06 §6、01 §12.10、ADR-0003 |
| P2-12 | 闭包捕获存容器生命周期 | 捕获双向登记 + 销毁 LIFO（Q26） | 06 §5、01 §12.9、CONTEXT、ADR-0003 |

### P3 一致性（低优先级小修）—— 4 项 ✅ 全部完成

| 编号 | 问题 | 处理 |
|---|---|---|
| P3-1 | 特性数量 25 vs 26 | 06 改 26 |
| P3-2 | API 命名（read_file/read_all 等） | 复查：语义不同非矛盾；04 新增「示例 API 清单」基线表 |
| P3-3 | 示例编号引用错误（6 处） | 已修正（46/61/63/71/75/84） |
| P3-4 | 空函数体占位示例 | 17/18 补 `return null;` 并标注草图 |

### P4 里程碑/排期 —— 2 项 ✅ 全部定案

| 编号 | 问题 | 裁定 |
|---|---|---|
| P4-1 | M3 ↔ 脚本 VM ↔ M4 循环依赖 | M3.0 脚本 VM 子集（单线程、无引用登记、无多线程）解环（Q28） |
| P4-2 | `mut` 占位符改名风险 | mut 定名（Q7）——占位符解除 |

## 三、裁定记录（Q1–Q28 摘要）

### 所有权与内存（Q1–Q3 系列）

- **Q1（P0-1）所有权按来源编译时判定**：非 Arena 分配器创建的复杂类型 → 所有权**默认注册当前作用域**（显式 `o` 为冗余标注）；Arena 分配 → 所有权归 Arena（禁 move，move 须对整个 Arena）。**可移动性（编译时判定）** = ① 来源条件（非 Arena）；② 树形内存条件：内存树**无外部引用**——无向外引用（字段及递归子项均无 `*T`/`*mut T`/`&[T]`/`&mut [T]` 引用值）+ 无向内引用（move 程序点无指向树内节点的存活引用，**编译期别名检查**）。「无向内引用」是 move 点的窄范围编译期别名分析，与运行时唯一写者登记正交。String/Vec 等自有子对象随树转移；Thread/Future/四模式类型豁免。
- **Q2（P0-2）struct 字段集放宽**：struct 字段可含复杂类型（String/集合/class），与 class 成员集一致（Q22）。纯 POD（字段全为标量/纯值 struct）→ 连续字节、可 `to_bytes` 直映射；含堆字段 → struct 本体内嵌字段值、序列化走递归。struct 与 class 差异收敛为：值语义（可内嵌、无分配器）vs 堆上引用类型（需分配器）。
- **Q3（P0-3）String = `u8[]` 别名**：String 具体功能与 `Vec(u8)` 一致，数组/切片规则全部适用（切片 = 数组视图）；**取代 Q16「String 值复制例外」**——恢复「无隐式内存分配」原则的无例外地位。
- **Q3c 引用类型赋值 = 别名共享**：`var s2 = s1;`（数组/集合/String）→ 共享同一数据；`s1` 保持唯一拥有（退出销毁一次）、`s2` 无所有权；深拷贝需显式 `copy`；别名 = 向内引用 → move 原变量编译期禁止；别名不得比拥有者长寿（逃逸检查）；所有权转移必须显式 `move`（赋值不隐含转移）。

### 语法规格（Q4–Q14）

- **Q4 运算符优先级**：Zig 式 14 级——后缀(1) > 一元(2) > 乘除模(3) > 加减(4) > 移位(5) > 位与(6) > 位异或(7) > 位或(8) > 比较(9，非结合) > `and`(10) > `or`(11) > `orelse`/`catch`(12) > `..`(13) > 赋值(14，右结合)；`try a catch b` = `try (a catch b)`。
- **Q5 字符字面量**：`'x'` 类型 = `comptime_int`（惰性宽度，使用处定型）；支持转义；无独立 `char` 类型。
- **Q6 转义集**：`\n` `\r` `\t` `\\` `\"` `\'` `\xNN` `\u{...}`；非法转义编译期报错；无原始字符串。
- **Q7 token 冻结 + mut 定名**：29 个关键字 + 标点清单冻结（见 06 §1）；`mut` 定名（占位符解除）；`box`/`copy` 为内建函数（非关键字）。
- **Q8 test 块**：`test "名称" {}` 顶层测试块；`hc test` 运行；失败 = error；`expect` 归 std.debug 测试块内隐式可用。
- **Q9 if 双向捕获**：`if (e!) |v| else |err|` 必须成对（错误显式处理）；作表达式时两分支值类型一致。
- **Q10 while 捕获**：`while (opt) |v|`（空则退出）、`while (e!) |v| else |err|`（错误走 else 后退出）；可与续步表达式组合。
- **Q11 忽略错误**：仅 `catch |_| {}` 形态，无 `catch {}` 简写。
- **Q12 box/copy 内建**：`box(value, alloc) -> o *mut T`（装箱）；`copy(&value) -> T`（按类型复制）；编译器内建、不可重载/用户定义、非关键字。
- **Q13 错误值**：`error.Name` 全局唯一；显式错误集函数 return 未声明错误 → 编译报错；`anyerror` 上下文任意可用。
- **Q14 四模式方法集**：`init(alloc)` / `write(v)` / `read() T`（阻塞）/ `try_read() ?T`（非阻塞）/ `close()`（结束标志）；方法取 `*Self`（Q32 内建共享特例）。

### 类型与引用（Q15–Q17）

- **Q15 `o` 适用范围**：仅**分配器分配的对象**（复杂类型/装箱指针）；标量/纯值 struct 写 `o` = 编译错误。
- **Q16 指针不可空**：`*T`/`*mut T` 无空态；空指针用 `?*T`/`?*mut T`；悬垂 ≠ null；无 NULL 字面量。
- **Q17 接口指针**：`*Shape` = 胖指针（data + 虚表，两字宽）；`box` 装箱后赋给接口指针**编译期自动收窄**（接口实现检查，无需 as）；data 部分参与双向引用登记（悬垂/唯一写者同 `*T`），虚表不参与。

### 并发与生命周期（Q18–Q21）

- **Q18 悬垂唯一路径**：引用逃逸到比目标更长寿的容器/全局（返回值引用被编译期禁止）；容器元素「取指针」不抛错、「解引用」才触发检测；28 示例重写。
- **Q19 线程捕获静态检查**：① 绑定/逃逸判定（数据流）——句柄在声明作用域内 join/await = 绑定（引用捕获合法），detach/未 join 退出 = 逃逸（引用捕获编译错误）；② **冻结窗口**——被捕获目标在 spawn→await 间主线程不可写（await 后恢复）；并发写共享数据走四模式类型。
- **Q20 Future 类型参数**：`async fn f(...) R` 返回 `Future(R)`，R = 完整返回类型（含 `!`）；`await` 返回 R（`try await` 解包）。
- **Q21 defer LIFO**：多 defer 后声明先执行（与资源创建顺序成镜像）。

### 接口与参数制（Q22 系列）

- **Q22 接口参数 = 虚拟类型 + where 约束**：`fn add(a: *T) void where T: INumber`；形态映射 `&T`→`*T` / `&mut T`→`*mut T` / `move T`→`o T`；调用点 `add(&a)` / `add(&mut a)` / `add(move a)`；静态单态化为主路径；`*Shape` 胖指针（Q17）用于异构集合。
- **Q22b 参数签名类型标注制**：参数签名统一 `o T`（拥有）/ `*T`（只读）/ `*mut T`（可写）；`move` 关键字仅保留在调用点（`take(move s);`）；此前「`move y: *mut T` 参数签名」表述取代。
- **Q22c io 参数**：库函数统一 `io: *T where T: Io`（调用点 `&io`）；入口 `fn main(io: Io)` 为编译器注入特例（唯一例外）。

### 元编程与解析（Q23–Q24）

- **Q23 script 输入机制**：script 块 = 编译前执行的 H 表达式块（H 核心子集），产物 = **生成代码字符串**就地替换；上下文隐式 `types` 元数据对象（`types.fields(name)` / `types.type(name)` / `types.all()`，编译器生成注入）。
- **Q24 `&` 三义**：lexer 只产出一个 `&` token；类型位置 = 切片类型 `&[T]`/`&mut [T]`；表达式位置 = 一元取地址（`&x`/`&mut x`）或二元位与（`a & b`）；`&mut` = `&` + `mut`；parser 上下文消解（LL(1) 可行）。

### 生命周期与销毁（Q25–Q26）

- **Q25 临时引用生命周期**：表达式产生的临时 `*T`/`*mut T` 存活到语句结束——方法链整链写者资格保持、链末一次注销；绑定引用存活到作用域结束；链内传递 = 同一写者资格延续（reborrow 形态，衔接开放问题 #2）。
- **Q26 闭包捕获登记 + 销毁 LIFO**：捕获变量与闭包建立双向注册（目标销毁 → 闭包标记悬垂，Debug 调用抛错带位置）；闭包存容器合法（登记链覆盖）；**作用域退出销毁顺序 = 声明逆序（LIFO）**。

### 一致性（Q27）与排期（Q28）

- **Q27 一致性小修**：06 特性数 25→26；6 处示例编号引用修正；17/18 空函数体补 `return null`；04 新增「示例 API 清单」基线表。
- **Q28 M3.0 脚本 VM 子集**：单线程解释器 = 词法/解析 + 求值 + 作用域所有权 + defer + 错误处理 + 集合/String + `types` 元数据；**延迟项**：引用双向登记、唯一写者、多线程/async（脚本内引用不检测）；脚本执行不受 M4 阻塞，M3/M4 可并行。

### 测试功能（Q-T1~Q-T6，2026-08-13 追加）

- **Q-T1 断言 API 五件套**（归 std.debug、测试块内隐式可用）：`expect(cond)` / `expect_eq(a, b)`（失败输出期望 vs 实际）/ `expect_neq` / `expect_error(e, expr)` / `expect_eq_slices`（长度 + 首个差异位置）；`expect_eq` 支持 `==` 可比较类型（含 String 内容比较）；均 `anyerror!void`。
- **Q-T2 输出与统计**：逐测试 `[PASS]/[FAIL]/[SKIP] 文件::测试`；FAIL 附错误类型 + 位置（Debug 附期望 vs 实际）；汇总 `N passed, M failed, K skipped (耗时)`；失败数 > 0 → 非零退出码；失败不中止（按文件分组，全跑完汇总）。
- **Q-T3 隔离/执行/跳过**：test 块独立作用域（退出自动销毁）；1.0 默认串行（并发留 1.x）；`return error.SkipTest;` → SKIP。
- **Q-T4 测试环境**：test 块内隐式 `test_io`（独立 `Io.threaded()` 实例，每测试创建/销毁）+ `alloc`；IO 测试默认真实执行。
- **Q-T5 双模式测试**：`hc test` 默认脚本模式；`--mode=compile` 编译模式跑同一套（M5 一致性套件交叉验证）；两模式语法/断言/统计完全一致。
- **Q-T6 示例测试形态**：S1 纯逻辑断言 / S2 main smoke（`try main(test_io);`）/ S3 局部逻辑（IO/网络示例测纯逻辑，不真跑网络）/ S4 演示标注（依赖外部环境，注释「断言留 1.x」）；输出不捕获（1.0）；85 示例 + math.hc 全部具备至少 1 个 test 块。

## 四、遗留开放问题（未覆盖）

以下条目本轮未触及，保留在 `05-open-questions-and-risks.md`：

1. 只读引用 Debug/Release 切换粒度（编译单元级/函数级/引用创建点级）
2. 函数借用参数的 reborrow 机制细则（Q25 已定方向：自动 reborrow；函数调用边界的写者资格过渡留 M4 细化）
3. 无 GC 下长运行脚本的内存增长模式（Arena 惯例）
4. 序列化与 schema 演进（版本字段/迁移钩子）
5. 官方注册中心治理
6. 跨线程引用传递（Send/Sync 静态标记与运行时簿记配合）
