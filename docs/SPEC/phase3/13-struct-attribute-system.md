# Struct 类型与特性系统 — 实施计划

> 设计定案：ADR-0022（2026-08-24 grilling 会话）
> 优先级排序：P0（必须）→ P1（重要）→ P2（锦上添花）→ P3（未来）

## 任务列表

### Phase 1: Struct 基础（P0）

| 序号 | 任务 | 预估时间 | 依赖 | 验证方法 |
|------|------|---------|------|---------|
| 1.1 | 添加 `KwStruct` token 变体 | 15min | 无 | 编译通过 |
| 1.2 | lexer 映射 `struct` → `KwStruct` | 5min | 1.1 | lexer 测试通过 |
| 1.3 | AST 添加 `Decl::Struct` 变体 | 30min | 无 | AST 编译通过 |
| 1.4 | 更新 `Trait` 枚举（删除 Continuous，Align 改数值） | 15min | 无 | 编译通过 |
| 1.5 | parser 添加 `KwStruct` 分支 | 15min | 1.1, 1.3 | 解析测试通过 |
| 1.6 | parser 实现 `parse_struct` 方法 | 30min | 1.5 | 解析 `struct Point { x: i32, y: f32 }` |
| 1.7 | parser 解析 struct 初始化表达式 | 30min | 1.6 | 解析 `Point{x=1, y=2.1}` |

### Phase 2: 语义分析 + 约束（P1）

| 序号 | 任务 | 预估时间 | 依赖 | 验证方法 |
|------|------|---------|------|---------|
| 2.1 | 语义分析：struct 类型注册 | 30min | 1.6 | struct 类型名可解析 |
| 2.2 | 字段类型约束检查 | 30min | 2.1 | 非法字段类型报错 |
| 2.3 | Align 属性处理（struct 级） | 30min | 1.4, 2.1 | `@alignOf` 返回正确值 |

### Phase 3: 内存管理（P1）

| 序号 | 任务 | 预估时间 | 依赖 | 验证方法 |
|------|------|---------|------|---------|
| 3.1 | 堆分配 `alloc.init(StructType)` | 30min | 2.1 | 堆上创建 struct 实例 |
| 3.2 | 装箱 `box` struct | 30min | 2.1 | 栈变量装箱到堆 |
| 3.3 | 字段默认值 | 30min | 1.6 | 默认值初始化 |

### Phase 4: 高级特性（P2）

| 序号 | 任务 | 预估时间 | 依赖 | 验证方法 |
|------|------|---------|------|---------|
| 4.1 | 字段级 Align 属性 | 30min | 2.3 | 字段对齐覆盖 struct 级 |
| 4.2 | 嵌套 struct | 30min | 2.1 | struct 字段为 struct 类型 |
| 4.3 | 特性系统字典注册 | 30min | 1.4 | 按名称查找特性处理器 |

### Phase 5: 扩展（P3）

| 序号 | 任务 | 预估时间 | 依赖 | 验证方法 |
|------|------|---------|------|---------|
| 5.1 | 扩展方法 `[Extension(T)]` | 45min | 1.6 | 方法调用 |
| 5.2 | `IAttribute` 接口 | 30min | 4.3 | 接口标记 |
| 5.3 | stage1 同步更新 | 30min | 1.1–1.7 | 语料测试通过 |

## 执行顺序

按序号顺序执行，每个子任务完成后必须：
1. 编译验证（`cargo build`）
2. 运行相关测试（`cargo test`）
3. 上传到 git（用户确认后）
4. 再继续下一个子任务