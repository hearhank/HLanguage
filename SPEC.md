# H 语言设计规格（SPEC）

一门以**数据生命周期**为第一原则的系统编程语言：先表示数据（定义、写、读、传输、存储），再谈行为。静态编译为主，可解释执行（双后端）。

## 设计原则

1. **数据优先**：语言的第一任务是表示数据——定义、写、读、传输、存储。行为（函数、组合）是作用于数据的工具层。
2. **一切皆字节**：所有数据（含函数）都能转换为字节数组。字节是语言的通用语。
3. **可见的机制**：无隐藏控制流。所有权移动、内存分配、生命周期通知都是显式的、成本可预见的。
4. **一致的语义**：同一份源码，编译与解释两种执行方式语义严格一致。

## 模块索引

| 模块 | 内容 | 文件 |
|---|---|---|
| 01 | 数据模型：两种内存形状（块/树）、嵌套、动态块、枚举 | [docs/spec/01-data.md](docs/spec/01-data.md) |
| 02 | 字节化：可逆格式、字节方法、函数字节化 | [docs/spec/02-byte.md](docs/spec/02-byte.md) |
| 03 | 生命周期：作用域、所有权、move、指针、双向引用、内存分配 | [docs/spec/03-lifecycle.md](docs/spec/03-lifecycle.md) |
| 04 | 并发：执行体、访问模式、跨执行体规则 | [docs/spec/04-concurrency.md](docs/spec/04-concurrency.md) |
| 05 | 行为层：函数契约、class、接口、错误、命名空间、类型推断 | [docs/spec/05-behavior.md](docs/spec/05-behavior.md) |
| 06 | 工具链：双后端、IR、命令、FFI | [docs/spec/06-toolchain.md](docs/spec/06-toolchain.md) |
| 07 | 与 Rust/Zig 功能对比：类型、生命周期、并发、字节化、能力矩阵 | [docs/spec/07-comparison.md](docs/spec/07-comparison.md) |

## 已定决策速览

- 数据两种内存形状：**块**（连续、复制语义）与**树**（非连续、带生命周期）
- 一切数据可转字节：块 = memcpy；树 = 序列化压平；格式**可逆、自描述**
- 生命周期 = 作用域树；**单一所有权 + 显式 move**；作用域退出销毁
- 指针只代理读写、不代理所有权；**可写指针强制双向引用且不跨执行体**
- 内存分配**显式 allocator**（Zig 式）；块默认栈上
- 执行体 = 线程/协程统一（数据 + 处理逻辑），M:N 调度
- 并发：**类型即模式**，四种访问模式用内建包装类型表达
- 行为层：函数为核心动词；**class = 组合**（接口声明处绑定、导入替代继承）；接口纯静态
- 错误：**error union + 可选值**，无异常
- 命名空间 = 文件系统即树；局部类型推断
- 工具链：**双后端**（共享前端 + IR，一致性硬承诺）；`h run` / `h build`；C ABI 互操作

## 状态约定

- 各模块末尾标注 **OPEN** 项：已承认但未定型的细节（语法示例、边界情况），不伪造答案。
- 术语定义见 [CONTEXT.md](CONTEXT.md)。
