# Language "H" (working name)

编程语言的核心是数据的生命周期：**定义数据 → 写数据 → 读数据 → 传输数据 → 存储数据**。本语言的第一个目标是让这五个操作成为语言的基石——先表示数据，再谈行为。

## 核心模型

**数据生命周期 (Data lifecycle)**:
编程的五项核心操作：定义（描述数据的形状）、写（创建/修改）、读（访问/查询）、传输（在进程/组件/机器间移动）、存储（持久化）。语言应首先表示这五件事。
_Avoid_: 以行为/控制流为中心的设计

**定义 (Define)**、**写 (Write)**、**读 (Read)**、**传输 (Transmit)**、**存储 (Store)**:
数据生命周期的五个阶段，每个都是语言的一等关注点（具体形态待定）。

**内存形状 (Memory shape)**:
从内存角度看，数据只有两种形态。

**块 (Block)**:
连续内存块（像 struct）。内容在内存中紧邻，转换字节数组是平凡操作（memcpy 级）。
_Avoid_: 聚合体（当指树时）

**树 (Tree)**:
非连续的数据（像 class），内容分散在不同内存位置，通过引用相连。树支持序列化，序列化后压平为块（byte 数组）。
_Avoid_: 对象图（除非确认同义）

**作用域 (Scope)**:
数据生命周期的容器：数据在作用域内生成、修改、传输、存储、销毁。程序本身是作用域的树形结构；退出作用域时，拥有所有权的变量必须销毁。作用域也是代码组织单元（树枝）：作用域内调用函数或嵌套作用域 = 树枝上长出新枝。

**作用域树 (Scope tree)**:
程序的整体结构：作用域嵌套成树。根作用域即全局。

**全局 (Global scope)**:
作用域树的根。定义在全局的数据所有权归全局所有，不可移动（move）；全局数据可读写。
_Avoid_: static（除非确认同义）

**所有权 (Ownership)**:
数据创建后具有的特性之一（另有读、写）。所有权移动必须显式使用 move 关键字；退出作用域时，作用域拥有所有权的变量必须销毁。与 Rust 的区别：移动是显式的（Rust 是隐式的）。

**读 (Read right)**、**写 (Write right)**:
数据的权限特性。拥有写权限才可以写，否则只读。指针可以代理这些权限。

**move**:
显式转移数据所有权的关键字。move 之后，所有指向该数据的指针失效，原变量也不能再使用。

**指针权限 (Pointer rights)**:
指针可以代理读/写权限，但写必须像 Zig 那样显式指定（const 限定）。指针只能代理读写，不能代理所有权（所有权只能整体 move）。
_Avoid_: 借用 (borrow)——除非确认同义

**执行体 (Execution unit)** (线程/协程):
统一概念：一个执行体 = 数据 + 处理逻辑的组合。是程序作用域树的一个分支，有自己的作用域结构。退出时，其拥有的数据自动销毁（若有所有权）。调度：M:N——语言级协作式执行体，映射到多个系统线程。执行体挂载于创建它的作用域（树枝）下运行；所在作用域退出时，执行体不随作用域销毁，而是提升注册到顶层根对象（程序级调度器），由根接管继续运行。
_Avoid_: 线程/协程分开称呼（语言层面统一）

**统一结构 (Unified concurrent structure)**:
全局数据的承载结构。程序提供所有可能的实现框架：既包括共享内存，也包括消息传递——具体用哪个由使用方选择。
_Avoid_: 单一并发模型（如只锁或只消息）

**访问模式 (Access pattern)**:
类型的一部分（类型即模式）：跨执行体共享的数据必须声明访问模式，编译器按类型分派实现。四种：单写单读（无锁通道）、单写多读（读写锁/共享不可变）、多写单读、多写多读（互斥/原子）。适用于单个数据与集合数据（数组等）。

**字节方法 (Byte methods)**:
定义在字节数组上的一批通用方法。由于一切数据都可转换为字节数组，这批方法在理论上适用于所有数据类型——字节数组是通用操作界面。
_Avoid_: 序列化库（当指特定库时）

**高级指针 (Advanced pointers)**:
保留指针，但采用 Zig 式高级指针（带类型、带长度、可空、const 限定），避免裸指针的典型问题。

## Language

**函数 (Function)**:
处理数据的方法：`A fun(B)`。传入参数三种：值类型（复制值）、引用类型（传指针）、移动变量（move 进函数）。返回可修饰：`move`（返回含所有权）、`ref`（返回可读写引用）、默认（只读的值）。语法：关键字标注——`x: T` 值复制（块）/只读指针（树）、`x: ref T` 可写指针、`x: move T` 转移所有权；返回 `-> move T` / `-> ref T` / `-> T`。捕获分三类：值捕获（块，可字节化）、move 捕获（树，可字节化）、ref 捕获（树引用，不可字节化）。
_Avoid_: 方法 (当指函数时)、过程 (procedure)

**变量声明 (Variable declaration)**:
Zig 式：可变性显式。可写变量才能声明可写指针（ref）；只读变量只能转移所有权（move）或创建只读指针。

**参数传递 (Argument passing)**:
块：默认值复制。树（class）：不标注时默认只读指针；标注 `move` 或 `ref` 选择转移所有权或可写指针。

**单一所有权 (Single ownership)**:
同一时间只有一个作用域拥有数据的所有权，因此借用（ref）期间数据不会被销毁——数据会正确销毁。

**执行体 (Execution unit)** (线程/协程):
统一概念：一个执行体 = 数据 + 处理逻辑的组合。是程序作用域树的一个分支，有自己的作用域结构。退出时，其拥有的数据自动销毁（若有所有权）。调度：M:N——语言级协作式执行体，映射到多个系统线程。执行体挂载于创建它的作用域（树枝）下运行；所在作用域退出时，执行体不随作用域销毁，而是提升注册到顶层根对象（程序级调度器），由根接管继续运行。
_Avoid_: 线程/协程分开称呼（语言层面统一）

**双向引用 (Bidirectional reference)**:
指针与数据之间互相知晓：数据销毁时通知所有指向它的指针使其失效，指针销毁时解除双向链接。保证无悬垂指针。机制是语言内建但可见的（显式运行时成本）。对堆上树数据（class/ref 字段）生效；随切片引入，也扩展到堆上块数据的数据区（动态块/Str 被切片借用时——销毁时通知切片置空，见 ADR 0008）。栈上的纯块（复制语义）不需要通知机制。可写指针必须双向引用且不跨执行体；只读指针的双向引用是**可选项**。

**元组 (Tuple)**:
块的一种（匿名块）：连续内存、值语义、元素必须块。位置元组 `(T1, T2)` 用 `.0/.1` 访问；命名元组 `(x: T1, y: T2)` 是带字段名的独立块类型，用 `.x/.y` 访问；两者禁止混用。支持声明/赋值解构 `(a, b) = f()`（逐元素可写性检查）。
_Avoid_: 匿名 struct（命名元组即匿名 struct，区别只在是否声明）

**切片 (Slice)**:
借用视图：不拥有的 `[ptr, len]`，只指向堆上动态块数据区（动态块/Str/动态块字段）。`[T]`（拥有动态块）↔ `[]T`（借用视图）：传参自动借用，反向显式 `clone()`。生命周期"借入不借出"（R12）：不得返回、不得入字段/全局、不得作 spawn 参数；数据销毁时运行时通知置空。
_Avoid_: 视图 (view)

**返回修饰 (Return modifiers)**:
`move`：返回类型包含所有权；`ref`：返回可读写的引用；无修饰：只读的值。

**接口 (Interface)**:
对一组函数签名的统一描述，用于把函数归类管理；不含数据或实现状态。纯静态：只存在于编译期，作为泛型约束，调用在编译期单态化；运行期不存在"接口值"。实现方式是显式的：组合声明实现某接口，并手动添加该接口定义的方法。
_Avoid_: trait, protocol, 抽象类

**组合 (Composition)**:
数据 + 方法集的聚合，提供类似 class 的功能（Go 风格功能集合），但无继承、无实例级动态方法。方法绑定在类型上，调用静态解析。可导入其他组合，自动获得其方法与接口标注，并可选隐藏。

**导入 (Import)** (组合对组合):
组合 A 导入组合 B 时，A 自动实现 B 的方法，并自动标注 B 所满足的接口；A 可以隐藏自动获得的方法和接口。同名冲突规则：A 自己定义的方法公开，导入的同名方法自动隐藏；多个导入对象之间的同名方法强制用户处理——必须隐藏或对指定对象的指定方法设别名。

**命名空间 (Namespace)**:
组织代码的树形结构单元：文件系统即树（文件 = 命名空间，子目录 = 子命名空间）。两级可见性：`pub`/私有。函数/接口/组合挂在其下。
_Avoid_: 模块 (module)、包 (package)——除非用户确认别名关系

**字节化 (Byte-form)**:
任何值（包括函数与组合）都能转换成一个字节数组。块 = 平凡 memcpy；树 = 序列化压平；格式可逆、自描述（递归嵌套 + 类型标签）。函数 = 代码引用 + 捕获环境，可序列化并恢复为可调用值（要求代码已加载、捕获值可序列化）。字节化是类型系统的性质而非过程——所有类型自动获得。
_Avoid_: 序列化 (serialization)——除非确认二者等价

**双后端 (Dual-backend)**:
同一份源码有两种执行方式：交给编译器产出原生二进制；交给解释器作为脚本执行。共享前端与共享中间表示（IR）；语义一致性是硬承诺——脚本跑通，编译后行为必须一致。
_Avoid_: 双语言、双前端

**脚本模式 (Script mode)**:
解释器执行源码的运行方式。定位为"特殊场景下的补充执行途径"，不是主要开发体验。内存管理与编译模式严格一致：显式 allocator、无隐藏控制流，语义一致性硬承诺在两种模式下都不放松。
_Avoid_: REPL（除非确认包含）、热重载

**手动内存管理 (Manual memory)**:
Zig 式内存管理：分配与释放显式、无隐藏控制流（无 GC、无隐式分配、无异常）。
_Avoid_: 垃圾回收、自动引用计数

**错误 (Error)**:
失败语义分两种：error union（Zig 式）表达"出错了"，可显式展开；可选值表达"预期缺席"（无此物）。无异常——异常即隐藏控制流。

**值语义 (Value semantics)**:
数据默认可变性的基石：值默认不可变，可变需显式声明；结构传参/赋值是值复制（memcpy，天然贴合字节化）。
_Avoid_: 引用语义、共享可变状态

**枚举 (Enum)**:
内建标签联合（tagged union）+ 穷尽性模式匹配（match），语言核心。错误集是枚举的特化。

**类型推断 (Type inference)**:
局部推断（Rust 式）：函数签名显式标注，函数体内允许推断。

**解释器形态 (Interpreter shape)**:
独立脚本执行为主（命令行直接运行源文件），预留宿主嵌入边界（字节数组作为与宿主的协议）。
