# H 语言 (H Language)

一门以数据为中心、同时支持系统编程与脚本编程的编程语言，源代码以 `.hc` 为后缀。设计的核心哲学是：语言的首要职责是「定义数据、修改数据、传输数据、保存数据」，一切语言特性围绕这四项第一性原则组织，并提供「编译为原生二进制」与「脚本直接运行」两种执行方式。

## 语言

**H语言 (H)**:
项目与语言的正式名称。
_Avoid_: H-Lang, HC 语言

**H源文件 (.hc)**:
以 `.hc` 为后缀的 H 语言源代码文件，是 H 程序的唯一源码载体。

**双模式执行 (Dual-mode execution)**:
同一份 H 源代码既可编译为原生二进制，也可作为脚本被解释执行；两种模式共享同一套语言语义，不存在「编译专用」或「脚本专用」的子语言。**语义一致 = 值语义、所有权语义、控制流一致**；错误检测策略是独立维度（Debug 全检测、Release 裸路径、脚本模式 Debug 语义，2026-08-13 收紧边界）。
_Avoid_: 编译模式与脚本模式割裂成两门语言

**系统编程 (System programming)**:
H 支持的一种使用场景：编写贴近硬件、性能敏感、可独立部署为原生二进制的程序。

**脚本编程 (Scripting)**:
H 支持的一种使用场景：编写可即时运行、无需显式构建步骤的小程序或自动化任务。

## 数据为中心 (Data-centric)

**定义数据 (Define)**:
声明数据类型、数据结构的形状与约束。数据为中心的第一支柱。

**修改数据 (Mutate)**:
对数据进行增、删、改等变换。数据为中心的第二支柱。

**传输数据 (Transmit)**:
数据跨进程、线程或网络边界的传递，包括序列化与 FFI。数据为中心的第三支柱。

**保存数据 (Persist)**:
数据的持久化存储，如文件与数据库。数据为中心的第四支柱。

## 设计原则

**唯一处理逻辑 (Functions as sole processing)**:
函数是数据为中心的**唯一处理逻辑**：一切数据处理通过函数表达；接口描述功能（处理能力的契约）；复杂类型 = 数据字段 + 函数的组合；无其它处理机制（无运算符重载、无宏）。
_Avoid_: 运算符重载, 宏

**无隐藏控制 (No hidden control flow)**:
语言不提供任何隐式控制流或隐式资源行为——无异常、无宏黑魔法、无隐式内存分配。唯一允许的隐式行为是作用域退出时的自动销毁（词汇可见、可预测）。一切控制流、资源与错误路径显式可见。
_Avoid_: 异常, 宏, 隐式分配
语言不提供任何隐式控制流或隐式资源行为——无异常、无宏黑魔法、无隐式内存分配。唯一允许的隐式行为是作用域退出时的自动销毁（词汇可见、可预测）。一切控制流、资源与错误路径显式可见。
_Avoid_: 异常, 宏, 隐式分配

## 类型系统

**显式初始化 (Explicit initialization)**:
变量必须显式初始化，无默认零值；未初始化即编译错误。
_Avoid_: 零值默认, undefined

**字面量惰性宽度 (Lazy literal width)**:
数字字面量采用 comptime_int/comptime_float：无固定宽度，在使用处定型，超范围编译期报错（Zig 式；2026-08-13 随 comptime 保留恢复完整语义）。
_Avoid_: 默认 i32/f64 假设

**可选值 (Optional)**:
`?T` 类型表达「可能没值」，使用前必须显式解包处理空态。与 error union（`E!T`「可能出错」）正交。
_Avoid_: null/undefined 陷阱

## 内存模型

**所有权 (Ownership)**:
所有权是一种注册关系：带 `o` 标记的变量，其所有权注册在当前作用域上，作用域退出时自动销毁并回收内存；不带 `o` 的变量，所有权注册在别处——通常是**分配器**（如 arena，显式分配、统一回收）或全局存储。**默认规则（2026-08-13 Q16 定案）**：复杂类型（由分配器分配内存创建），除 Arena 外默认拥有所有权（o 隐含）——添加到作用域后作用域接管，退出时销毁其分配的内存；Arena 分配的无所有权（统一销毁）。**无 `o` 变量禁止 move**（move 须对整个分配器进行）。仅带 `o`（本作用域拥有）的变量可以 move。
_Avoid_: 引用计数, 手动 free

**作用域 (Scope)**:
所有权与生命周期的基本单位。每个块是一个作用域（可视为没有名字的函数），函数是有名字的作用域。作用域退出即销毁其拥有的变量。
_Avoid_: 用「函数」泛指所有作用域

**move**:
将变量的所有权从一个作用域显式转移到另一个作用域（函数参数或返回值）的操作；仅对带 `o`（本作用域拥有）的变量合法。转移后原作用域退出时不再销毁该变量，由目标作用域负责。不复制数据。
_Avoid_: 把 move 当作复制

**全局变量 (Global variable)**:
用 `global` 声明的变量，位于根作用域上下文，静态生命周期（程序运行期间存活）；默认不可 move，不带 `o`。
_Avoid_: 把全局变量当作普通作用域变量

**数据栈对象 (Data-stack object)**:
自包含的连续内存值类型（基础类型、定长数组、仅含值类型字段的结构体），传参默认复制语义。
_Avoid_: 把含引用或堆数据的类型当作数据栈对象

**只读引用 (Read-only reference)**:
对目标变量的只读访问视图，不授予修改权。与目标之间是可选双向引用关系，是否注册为编译期选项：Debug 构建默认注册（快速定位悬垂风险），Release 构建默认裸读（悬垂属用户责任）。
_Avoid_: 把只读引用当作无限制别名

**读写引用 (Read-write reference)**:
对目标变量的可写访问视图（可写包含可读）。与目标之间是强制双向引用关系：目标销毁时引用被标记悬垂，访问悬垂引用抛出携带出错位置的错误。同一变量同一时间最多一个读写引用（唯一写者），由运行时登记强制；**wr 引用不可拷贝，只能 move 传递**（Rust 式 &mut 语义）。
_Avoid_: 裸指针

**悬垂检测 (Dangling detection)**:
已注册双向关系的引用在目标销毁后被标记为悬垂，访问时抛出带出错位置的错误。未注册的只读引用悬垂属用户责任（未定义行为）。
_Avoid_: 把所有悬垂都当作未定义行为

**切片 (Slice)**:
`&[T]`（只读）/ `&mut [T]`（可写）——`*T`/`*mut T` 指针的带位置（起始指针）与长度的形态，对连续内存（通常为定长数组）的只读或可写视图。不拥有数据（无 `o`）；操作与指针一致（双向注册、悬垂检测、唯一写者）；move 只针对其底层数组。
_Avoid_: 把切片当作数据栈对象

**数组与集合 (Arrays and collections)**:
定长数组、变长数组、集合（Vec/Map/Set 等）为**引用类型**：传参走引用，复制需显式（`copy`）——2026-08-13 定案（评审 B3）。标量类型保持值语义。
_Avoid_: 数组默认复制传参

**defer**:
在创建/获取资源处显式声明的清理语句：作用域退出时执行（Zig 式）。`errdefer` 仅在错误返回路径执行。保证清理动作在创建处可见（不用隐式析构）。
_Avoid_: 隐式 RAII 析构（H 不用）

## 并发模型

**线程访问模式 (Thread access pattern)**:
多线程共享数据的**内建泛型类型族**（共享内存容器）：`OneToOne(T)`（单读单写）、`OneToMany(T)`（单读多写）、`ManyToOne(T)`（多读单写）、`ManyToMany(T)`（多读多写）。写者数量由类型名保证（单写者无锁路径、多写者互斥）；用泛型（脚本生成）插入数据类型。
_Avoid_: 裸 Mutex/通道二选一

**线程所有权 (Thread ownership)**:
任何作用域可开启线程，所有权默认归当前作用域。作用域退出时：线程已完成 → 销毁后退出；仍在运行 → 所有权移交根作用域管理。
_Avoid_: 隐式 join 阻塞

**线程捕获 (Thread capture)**:
线程捕获参数的规则：值类型 → 复制值；引用类型 → 必须 move 所有权，或为根作用域上下文中的 global 变量（global 不可 move）。**作用域例外（2026-08-13 Q18 定案）**：作用域绑定的执行（异步任务 `await`/`join` 后回到当前作用域）可捕获引用；可能逃逸的线程（运行中移交根作用域）引用捕获禁用。
_Avoid_: 闭包式只读捕获用于逃逸线程

**Future**:
异步任务的结果句柄（复杂类型）。`async` 函数返回 `Future(T)`；`await` 等待其完成（2026-08-13 定案：async/await 为语言关键字，逆转此前 Zig 式的无关键字决策）。基于线程任务（默认）或事件循环实现；执行语义待细化。
_Avoid_: 把 await 当作隐藏阻塞（显式语义）

**结构体 (Struct)**:
数据类型的定义单元：值类型、底层为连续内存（实质是字节数组）、无继承（组合优于继承）。字段只含标量、struct、定长数组、切片（不含复杂类型字段）；字段默认只读，`mut` 修饰可写（Rust 标杆，2026-08-13）。方法即声明在结构体内的普通函数（Zig 式）。字段全为纯值时为数据栈对象（可复制）；含切片字段时归其它对象（只能引用/move）。
_Avoid_: 类, 继承

**复杂类型 (Complex type)**:
堆上分配的类型，用关键字声明（1.0 最小集：`class` 通用堆对象、`tree` 递归/层级组合——父 `o` 拥有子，家族可扩展）；底层为连续或不连续内存；组合形式：字段可含标量、struct、其它复杂类型，字段可带 `o`（拥有其字段，随类型销毁）。构造 = 普通函数返回实例；无隐式析构（资源清理走 defer）。区别于 struct（值类型）。
_Avoid_: 把所有堆类型都叫 struct

**接口 (Interface)**:
描述类型功能的契约集合（方法签名），**显式声明实现**（Rust 式）。可描述复杂类型、标量类型、内建类型、用户定义 struct。不提供运算符重载。
_Avoid_: 把接口当抽象基类

**脚本生成 (Script generation)**:
元编程机制之一：源码中用 `script` 作用域标注脚本代码，编译前由脚本解释器执行并生成代码（对应 Zig 的编译时生成，用脚本表达）。脚本用 H 编写；解释器同时支持实时预览与校验。与语言内 comptime 式功能**并存**（分工已定：comptime 管类型级计算——泛型/类型即值；脚本生成管样板与数据定义驱动的代码生成）。
_Avoid_: 把脚本生成当作唯一元编程机制

**枚举类 (Enum)**:
「定义数据」的类型构造：标签 + 可选负载的合一声明（`enum Value { int: i32, none }`）。纯常量枚举 = 变体均不带负载的特例。匹配用 switch 穷举 + 负载捕获（`|x|` 惯例）；数据栈对象判定与结构体一致。
_Avoid_: 分离的 enum 与 union

**Allocator**:
显式的内存分配器实例。分配内存的代码显式传入 Allocator；未传入时回退到全局分配器。销毁由所有权作用域负责。
_Avoid_: 隐式作用域级分配器
