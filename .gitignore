# 依赖
node_modules/

# H 语言 C 编译产物（zig cc / gcc 生成）
*.exe
*.pdb
*.c.bak
*.o
*.bin
*.lock
# 库形态构建产物（C3：zig ar 静态归档 + 符号表；C4：dll 动态库 + 导入库）
lib*.a
*.dll
*.lib
*.sym
build/
out/
debug/
target/

# 临时文件
*.tmp
*_tmp.*
*.log

# Zed 扩展部署产物（由 deploy-lsp.bat 重新生成）
extensions/zed/extension.wasm
setup-lsp.bat
/zed-lsp-snippet.json
# Zed 语法 grammar 运行时克隆/编译目录（grammars/h.wasm 与 grammars/h/）
extensions/zed/grammars/

# 编辑器/系统
.DS_Store
Thumbs.db
nul

# Tool
# .claude/
.Zed/
.*/

# 调试/测试临时产物（2026-08-29 约定）：一律生成到 stage1/k4test/，纯临时结果用后即删；tag1 仅作对照基准
/tag1/*.txt
/tag1/*.chk
/tag1/*.ast
/tag1/*.py
/tag1/*.log
