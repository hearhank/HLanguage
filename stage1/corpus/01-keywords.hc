// 全部 45 关键字（Rust 参考：struct→KwClass，and→KwAnd）
var const fn global if else while for break continue return switch defer errdefer
class struct enum union tree interface where namespace using import pub export
owned move mut and or try catch orelse script comptime anytype type async await
spawn void null true false
