id = "h-language"
name = "H Language"
version = "0.1.0"
schema_version = 1
description = "H language support for Zed"
repository = "https://github.com/h-language/h"
authors = ["Hank <baibib@163.com>"]
themes = []
icon_themes = []
languages = ["languages/h"]
capabilities = []

[lib]
kind = "Rust"
version = "0.7.0"

[grammars.h]
# Dev note: Zed checks out `repository` at `rev` and builds src/parser.c.
# After editing grammar.js / queries: run `npx tree-sitter generate`, then
# COMMIT the grammar files before reinstalling the dev extension.
repository = "file:///C:/Users/Hank/Documents/works/AI/H2"
rev = "feature/self-host-k5"
path = "extensions/zed/languages/h"

[language_servers.hc-lsp]
languages = ["H"]

[language_servers.hc-lsp.language_ids]

[context_servers]

[agent_servers]

[slash_commands]
